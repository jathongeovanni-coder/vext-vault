[build]
target = "index.html"
dist = "dist"
public_url = "/"

# CRITICAL: This tells Trunk to take everything in your /public folder
# and copy it directly into the /dist folder that Vercel deploys.
[[build.copy_public]]
from = "public"

[watch]
ignore = ["dist"]

[serve]
port = 8080
open = false